-# ユーザーの詳細ページでそのユーザーが誰に対していいねしているのかどうかを表示
%h1 ユーザー詳細ページ
%h3= @user.name
%h2 投稿内容
- @user.posts.each do |post|
  %a{:href => "/posts/#{post.id}"}= post.content
  %hr/
%h3 いいねしている投稿
- @user.liked_posts.each do |post|
  %li
    - if post.user.name?
      = post.user.name
    - else
      noname
    = post.content
= link_to "ユーザー一覧へ", users_path
= link_to "ホームへ戻る", posts_path